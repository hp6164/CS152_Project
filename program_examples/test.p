
:main () num
{
    num a.
    num b.
    num c.

    $ Function fibonaci not declared
    $ num err = fibonaci().

    $ variable not declared
    $ erro = 10.
    
    $ Using a variable without declaring
    $ print(erro).

    $ Array of -ve size
    $ list errr[-1].

    $ Defining a variable more than once -- working
    $ num b = 2.

    $ Trying to name a variable with the same name as a reserved keyword.
    $ num printL.

    $ Use an array variable as a regular integer variable
    $ list k.
    $ b = a + k.

    $ Use a regular integer variable as an array variable
    $ c = b[1].

    $continue not in loop.
    $contn.

    $ret 0.
}